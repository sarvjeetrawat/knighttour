<!-- res/drawable/ic_splash_knight_animated.xml -->
<!--
    ═══════════════════════════════════════════════════════════════
    KNIGHT TOUR — ANIMATED SPLASH KNIGHT (API 31+)
    AnimatedVectorDrawable — scales up + fades in the knight icon.
    Duration: 800ms (matches windowSplashScreenAnimationDuration)
    ═══════════════════════════════════════════════════════════════
-->
<animated-vector
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:aapt="http://schemas.android.com/aapt"
    android:drawable="@drawable/ic_splash_knight">

    <!-- Scale the entire drawable from 0.5 → 1.0 -->
    <target android:name="root">
        <aapt:attr name="android:animation">
            <set>
                <objectAnimator
                    android:propertyName="scaleX"
                    android:valueFrom="0.5"
                    android:valueTo="1.0"
                    android:duration="800"
                    android:interpolator="@android:interpolator/overshoot" />
                <objectAnimator
                    android:propertyName="scaleY"
                    android:valueFrom="0.5"
                    android:valueTo="1.0"
                    android:duration="800"
                    android:interpolator="@android:interpolator/overshoot" />
                <objectAnimator
                    android:propertyName="alpha"
                    android:valueFrom="0"
                    android:valueTo="1"
                    android:duration="400" />
            </set>
        </aapt:attr>
    </target>

</animated-vector>